import { SupabaseAuthGuardGuard } from './supabase-auth-guard.guard';

describe('SupabaseAuthGuardGuard', () => {
  it('should be defined', () => {
    expect(new SupabaseAuthGuardGuard()).toBeDefined();
  });
});
